"use client";
import { Loader } from "rsuite";

const LoadingPage = () => {
  return (
    <main className="error-page">
      <Loader content="Loading..." />
    </main>
  );
};

export default LoadingPage;
